# Release Notes v1.2

## 🎯 Основні зміни

### 1. **Навігація для неавторизованих користувачів**

- **Файли**: `Header.jsx`, `Nav.jsx`
- **Зміни**:
  - Навігаційне меню тепер видиме для всіх користувачів, незалежно від статусу авторизації
  - Кнопка "Додати послугу" відображається тільки для авторизованих користувачів
- **Мета**: Покращення UX для незареєстрованих відвідувачів

### 2. **Виправлення Cumulative Layout Shift (CLS) на Footer**

- **Файли**: `Footer.module.css`
- **Зміни**: Додано `min-height: 150px` до footer елемента
- **Результат**: Усунуто зсув макета при завантаженні сторінки (CLS score покращився на 0.159)

### 3. **Оптимізація кешування статичних ресурсів**

- **Файли**: `vercel.json`
- **Зміни**: Додано Cache-Control headers для:
  - `/assets/*` - 1 рік (`max-age=31536000, immutable`)
  - Зображення (jpg, jpeg, png, gif, svg, webp, ico) - 1 рік
  - JS та CSS файли - 1 рік
- **Мета**: Покращення PageSpeed Insights scores, зменшення навантаження на сервер

### 4. **Вертикальне вирівнювання Logo в Header**

- **Файли**: `Header.module.css`
- **Зміни**: Додано `display: flex` та `align-items: center` до `.leftSection`
- **Результат**: Logo правильно вирівняний по вертикалі в header

### 5. **Видалення браузерних alert повідомлень для геолокації**

- **Файли**: `LocationPicker.jsx`
- **Зміни**: Видалено всі `alert()` виклики при помилках геолокації
- **Результат**: Більш чистий UX, помилки обробляються через callback `onMapError`

### 6. **Автоматичне скидання scroll позиції при навігації**

- **Файли**:
  - `ScrollToTop/ScrollToTop.jsx` (новий компонент)
  - `SharedLayout.jsx` (інтеграція)
- **Зміни**:
  - Створено компонент ScrollToTop з `useLayoutEffect`
  - Автоматичне скидання scroll до верху при зміні роуту
  - Використовується `behavior: "instant"` для миттєвого переходу
- **Результат**: Користувачі завжди бачать верх сторінки при переході між роутами

### 7. **Запам'ятовування позиції категорії**

- **Файли**:
  - `CategoryItem.jsx`
  - `Category.jsx`
- **Зміни**:
  - При кліку на категорію - ID зберігається в `sessionStorage`
  - При поверненні на Головну - автоматичний scroll до обраної категорії
  - Категорія центрується на екрані з плавною анімацією
  - Після scroll - збережений ID видаляється
- **Результат**: Покращена навігація - користувач повертається до обраної категорії

### 8. **Підготовка до 404 сторінки**
